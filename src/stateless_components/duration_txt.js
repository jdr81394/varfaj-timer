import React from 'react';

const DurationText = ({text}) => {
    return (  
        <div style={{ width: "30%", margin: "auto", textAlign: "center"}}> {text} </div>
    );
}
 
export default DurationText;